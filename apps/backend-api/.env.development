# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=athena

# Application Configuration
PORT=3000
NODE_ENV=development

# Security - REQUIRED: Change this in production
ENCRYPTION_KEY=your-secret-encryption-key-change-in-production

# API Configuration
API_PREFIX=/api
# CORS_ORIGIN examples:
# Development: http://localhost:5173 (Vite dev server)
# Production: https://yourdomain.com or * for any origin
CORS_ORIGIN=*
