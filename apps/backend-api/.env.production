# Production Database Configuration
DB_HOST=192.168.103.76
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=nfvisionaire123
DB_NAME=athena

# Application Configuration for Production
PORT=3000
NODE_ENV=production

# Security - Production encryption key
ENCRYPTION_KEY=prod-secure-encryption-key-change-this-in-actual-deployment

# API Configuration
API_PREFIX=/api
# Allow all origins for now, restrict in actual production deployment
CORS_ORIGIN=*
